name: test_app
description: Professional Test Generation and Tracking App
publish_to: 'none'

version: 1.0.0+1

environment:
  sdk: '>=3.6.0 <4.0.0' # Bumped minimum Dart SDK constraint to support flutter_tex 5.2.7

dependencies:
  flutter:
    sdk: flutter
  provider: ^6.1.2
  shared_preferences: ^2.2.3
  fl_chart: ^0.65.0
  collection: ^1.18.0
  flutter_tex: ^5.2.7

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.2

flutter:
  uses-material-design: true
